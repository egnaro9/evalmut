{
  "applicability_predicate": "Any framework offering a negation prefix implemented per handler rather than centrally. The predicate is: an assertion and its negation return the same verdict on the same input.",
  "applicable_to_evalmut": true,
  "counterexample": "A handler that legitimately has no meaningful negation, where the framework refuses the `not-` form outright, is correct and must not be flagged.",
  "id": "CB-004",
  "maps_to_existing_operator": "none. A negation-parity operator does not exist in evalmut.",
  "not_applicable_cases": [
    "frameworks that invert centrally after the handler returns",
    "assertions with no negated form"
  ],
  "pair": {
    "clean_input": "assert levenshtein(output, ref) <= threshold  -> PASS on a near-identical output",
    "defective_transformation": "assert NOT-levenshtein on that same near-identical output",
    "expected_on_clean": "PASS.",
    "expected_on_defective": "FAIL. The negation must invert the verdict."
  },
  "risks": {
    "false_negative": "Testing one handler proves nothing about the other handlers with the same shape. PR 9738 is the identical defect in `moderation`, found separately.",
    "false_positive": "Reporting a hole where the framework deliberately rejects negation."
  },
  "semantic": "`handleLevenshtein` never read the `inverse` flag that the framework derives from the `not-` prefix, and there is no generic post-handler inversion, so `not-levenshtein` behaved identically to `levenshtein`.",
  "source": {
    "author": "i-anubhav-anand",
    "authored_by_egnaro9": false,
    "merge_commit": "560ea2d236e7",
    "pr": 9722,
    "repo": "promptfoo/promptfoo",
    "url": "https://github.com/promptfoo/promptfoo/pull/9722"
  },
  "status": "CANDIDATE, not independently reviewed",
  "title": "not-levenshtein ignored the inverse flag, so negation was a no-op",
  "verdict_class": "wrong-verdict"
}
